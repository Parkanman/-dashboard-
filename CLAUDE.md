# CLAUDE.md — Дашборд ИДУ Pro · Альфа Капитал

## Проект

Одностраничный HTML-дашборд для внутреннего использования команды ИДУ Pro (Индивидуальное Доверительное Управление) Альфа Капитал.

**Файл:** `index.html` (~6200+ строк)  
**Репозиторий:** https://github.com/Parkanman/-dashboard- (ветка main, деплой GitHub Pages)  
**Стек:** чистый HTML/CSS/JS + Chart.js (CDN). Никаких фреймворков.  
**Данные:** localStorage + часть live через MOEX API.

---

## Брендбук Альфа Капитал v.1 2026

**Полный документ:** `c:\Claude Code\Отчет клиентам\Бренд  Бук\alfa-capital-design-system (1).md`  
**ОБЯЗАТЕЛЬНО** проверять соответствие — пользователь строго соблюдает цвета.

### Базовые цвета UI

| Цвет | HEX | Применение |
|------|-----|-----------|
| Капитальный красный | `#E61E14` | Акцент, кнопки, основные данные |
| Финансовый графит | `#494C53` | Фоны секций, текст |
| Стратегический чёрный | `#1E1E1E` | Фоны тёмных секций |
| Доверительный синий | `#1E62D2` | ≤5% площади, UI-акценты, border-top секции navy |
| Доходный зелёный | `#00BA38` | Положительные значения, прибыль |
| Портфельный бежевый | `#FAD2B4` | Пока не используется |

### Data Viz — палитра «герой + свита» (секции 01–04)

Принцип: первая серия — красный-герой, остальные — приглушённые, не конкурируют.  
**Секция 05 (Эмитенты) — особые правила:** корпоративные цвета эмитента + брендбук.

| # | HEX | Название | Применение |
|---|-----|----------|-----------|
| 1 | `#E61E14` | Капитальный красный | Первая серия, hero-метрика |
| 2 | `#5B7B9A` | Приглушённый стальной | Вторая серия, облигации |
| 3 | `#C4956A` | Тёплая бронза | Третья серия, commodities |
| 4 | `#7B8E7B` | Шалфейный | Четвёртая серия, стабильность |
| 5 | `#8A8D91` | Нейтральный серый | Бенчмарк, baseline, «прочее» |

⚠️ Запрещено: «радуга» из ярких хроматических цветов — для HNW-аудитории выглядит как детская инфографика.

---

## Архитектура

**CSS Grid:** 12 колонок, классы `col-4`, `col-5`, `col-6`, `col-7`, `col-8`, `col-12`.

**Темы секций:**
- `section-platinum` → белые карточки
- `section-graphite` → карточки `#5A5E68`
- `section-black` → карточки `#272727`
- `section-navy` → карточки `#494C53` + `border-top: 3px solid #1E62D2`

**Sticky mini-nav:** 5 ссылок (01–05). Активная секция через `window.scrollY + 120`. Последняя секция — atBottom-логика. При добавлении секций обновлять массив `sections` в IIFE навигации.

**Все чарты:** `maintainAspectRatio: false` везде. Кастомные плагины `afterDraw` для меток и нулевых линий.

---

## Критические правила

- **НЕ использовать `prompt()`** — не работает в VSCode Simple Browser (file:// протокол). Только кастомные `<dialog>` с `.showModal()`.
- **`month-bands` плагин:** ширину полосы считать через `(area.right - area.left) / chart.data.labels.length` — НЕ через `getPixelForTick` (возвращает только видимые тики).
- **Цвета в чартах тёмных секций:** оси `rgba(255,255,255,.45)`, grid `rgba(255,255,255,.06)`.
- **Цвета в чартах светлых секций:** оси `color: '#929498'`, grid `rgba(0,0,0,.08)`.
- **badge.flat** на тёмных карточках: нужны CSS-оверайды для `.section-black .card .badge.flat` и `.section-graphite .card .badge.flat`.

---

## Секции дашборда

### 01 / Рыночные котировки (`section-platinum`)
- Watchlist (col-4): IMOEX, USD/RUB, BRENT $, BRENT ₽, RGBI — live MOEX API, каждые 60 сек
- Акции (col-8): live таблица котировок
- Индикаторы настроения (col-6): RVI, импульс IMOEX 30д, спред ОФЗ/ставка ЦБ, gauge-chart
- Ставка ЦБ и ОФЗ-10л: ввод через `<dialog id="rate-input-dialog">`, функции `openRateDialog()` / `saveRateDialog()`
- Макропоказатели (col-6): 10 ячеек, редактируются через `openMacroIndDialog()`

### 02 / Макро статистика (`section-graphite`)
- Недельная инфляция ИПЦ (col-5): бар-чарт 12 мес.
- КС + ставки вкладов (col-6): combo-чарт
- Средства ФЛ в банках (col-6): линия + масштаб рынка
- Кредитный импульс, инфляционные ожидания, P/E рынка (col-12)
- Притоки в ПИФы (col-6): денежный рынок `#E61E14`, облигации `#00BA38`
- Притоки/оттоки фондов акций + IMOEX (col-6)
- Визуальные группировки через `.grid-group-hd`: «Кредит и реальная экономика», «Оценка рынка», «Потоки капитала»

### 03 / Бизнес-показатели Pro-сегмент (`section-black`)
- Лиды + клиенты (col-7): **50 лидов, +3 за последние 2 недели**, 44 клиента
- AUM (col-5): 2,94 млрд (через июнь 2026)
- Выручка/маржинальность (col-12): бар+линия по кварталам

### 04 / Клиентский портфель (`section-navy`)
Мультиклиентный режим: кнопки-вкладки, функция `switchClient(id)`, скрывает/показывает `#c1-data`, `#c2-data`, `#c3-data`. Ленивая инициализация чартов: флаги `c2ChartsInited`, `c3ChartsInited`.

- **Клиент №1** (Петров Р.В., 55,56 млн): накопленная доходность vs IMOEX, waterfall атрибуции, donut структуры, тепловая карта, таблица ОФЗ
- **Клиент №2** (Науменко): доходность vs IMOEX
- **Клиент №3** (Ишмяров, 191,4 млн): текущая структура портфеля + предложение

### 05 / Эмитенты (`section-black`)
Переключатель: кнопки `.issuer-tab`, функция `switchIssuer(id)`.

**Сбербанк (SBER):** данные из пресс-релиза 5М 2026.  
- Чистая прибыль 2026 = `#00BA38`, 2021–2025 = серые оттенки (темнее → старее)
- Кредитный портфель: корп `#5590E8`, розница `#E8800A`

---

## localStorage ключи

| Ключ | Содержимое |
|------|-----------|
| `ak-cbrate` | Ставка ЦБ |
| `ak-ofzrate` | Доходность ОФЗ-10л |
| `ak-leads-2026` | Лиды |
| `ak-macro-ind-v1` | Макропоказатели |
| `ak-cpi-weekly` | Недельная инфляция |
| `ak-biz-clients-v3` | Клиенты бизнеса |
| `ak-biz-aum-v3` | AUM по месяцам |
| `ak-revenue-2026` | Выручка |

---

## Вычеркнуто навсегда (решение команды)

- Коэффициент конверсии лидов → клиентов
- Расчёт доходности ОФЗ к погашению (нет дат покупок)
- Timeline купонных выплат
- Лесенка погашений ОФЗ
- Блок «Ближайшие события»
